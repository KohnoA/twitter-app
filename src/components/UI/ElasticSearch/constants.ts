import { ICONS } from '@/constants';

export const DEFAULT_PLACEHOLDER = 'Search';
export const INITIAL_VALUE = '';
export const { SpinnerIcon, SearchIcon, CrossIcon } = ICONS;
export const DEFAULT_EMPTY_MESSAGE = 'Not Found';
