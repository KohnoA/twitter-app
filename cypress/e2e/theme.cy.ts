describe('Theme change testing', () => {
  before(() => {
    cy.logout();
    cy.login();
  });

  it('', () => {});

  it('save theme value after page reload', () => {});

  after(() => cy.logout());
});
